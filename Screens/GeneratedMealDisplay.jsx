import { StyleSheet, Text, View } from 'react-native';

export default function FoodDisplayCard ({bgColor}) {
    return (
        <View style={
            backgroundColor=bgColor
        }>

        </View>
    );
}

const styles = StyleSheet.create({
    
})